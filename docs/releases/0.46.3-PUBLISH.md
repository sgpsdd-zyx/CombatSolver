# 0.46.3 发布记录

[版本索引](README.md) · [完整中英更新日志](0.46.3-RELEASE_NOTES.md) · [合并与验证](../strategy/upstream-0462-merge-20260924.md)

2026-09-24 用户明确要求“好的，发布release”。[GitHub Release 0.46.3](https://github.com/sgpsdd-zyx/CombatSolver/releases/tag/v0.46.3) 已创建为正式最新版并上传最小 ZIP；多人 fork 的任务分支与 annotated tag 已原子推送。本次公开基线为 fork 0.45.1，官方基线为 `4bfb4407 / 0.46.2`。

## 发布来源与直接凭证

| 阶段 | 完成证据 |
| --- | --- |
| 发布来源 | `02d1523c0909b0b5fb19dd93b76e9692f8b83dec`；行为来自已验证的合并提交 `988d8cd5d001267c409161525e3481145668172c`，本次只增加版本、说明及发布元数据。 |
| 版本选择 | 官方 annotated tag `v0.46.2` 固定于 `4bfb4407`，发布前本地和 origin 没有 `v0.46.3`。新标签 `v0.46.3` 固定于上述发布来源，未移动官方标签。 |
| 行为验证 | 复用[合并证据](../strategy/upstream-0462-merge-20260924-evidence.json)：八次初始原生请求、最终 UI 本地化复跑、25 项运行库配置合同及结构门禁。发布阶段没有重复这些场景。 |
| 客户端配置来源 | 从本机已安装的官方工坊 0.46.2 DLL 只读取五项客户端连接元数据；存在性、HTTPS 与证书指纹格式通过。配置仅写入被忽略、权限受限的私有 props，未输出值、读取服务管理凭据或请求线上服务；`connection-source.json`。 |
| 定版构建 | 发布来源执行一次 Release 构建，显式启用 `PublicationBuild=true` 并指定私有配置目录，0 警告/0 错误，10.22 秒；`build.log`、`build.json`。 |
| 新 DLL 连接检查 | 五项连接元数据及 HTTPS、指纹格式通过；仅定向读取程序集元数据，不载入游戏或访问端点；`connection-final.json`。 |
| 本地部署 | 同次构建的 DLL、manifest、LICENSE 和 THIRD_PARTY_NOTICES.md 精确覆盖已确认的 `SlayTheSpire2.app/Contents/MacOS/mods/CombatSolver`，不改其他 Mod；`deployment.json`。 |
| 最小 ZIP | `releases/CombatSolver-0.46.3.zip`，2,246,843 字节，仅包含最终 manifest、新 DLL 与两份许可文件；`preparation.json`。macOS 不生成 Windows MemoryCleaner，未从旧包或官方 DLL 反向取发布二进制。 |
| 分支与标签 | `codex/multiplayer-advisor` 从 `fdbe4e6c` 推送至发布来源，与新 annotated tag 一次原子推送成功；`git-push-release.log`、`tag.json`。 |
| GitHub Release | 显式指定 `sgpsdd-zyx/CombatSolver`、验证已有标签、上传最小 ZIP，以完整中英玩家说明创建正式最新版；`github-release-create.log`。 |
| 发布后归档 | README、AGENTS、版本/策略索引、多人指南及开发/测试记录同步已发布状态；纯文档提交位于标签之后，推送凭证保存为 `git-push-closeout.log`。 |

本地阶段凭证位于 `.local/release-0.46.3/`，私有配置及临时读取工具不进入源码提交或发布 ZIP。构建只嵌入客户端连接所必需的信息，与官方发行文件使用同一在线服务；单人受已有“在线统计”设置控制，多人和无头会话沿用不上报边界。

发布前 L0 核对覆盖 10 份 Markdown、805 个本地文件链接、每种语言六项说明、版本一致性与私有配置提交边界。官方 0.46.0、0.46.1、0.46.2 更新日志原文保留；PR #131 和 #132 的贡献者主页与 PR 链接位于对应中英文条目开头。

## 范围与保留项

发布范围为 fork 的 GitHub 分支、标签和 Release；官方 main、Steam 创意工坊、夸克网盘及后台最新版提示不属于本次交付。线上连接请求、可见 UI/性能、真实联机、Windows/PowerShell、Linux 原生启动器、干净安装和完整发布门禁未验收；客户端配置检查不是在线服务可用性或联机质量证明。

每阶段采用一次成功命令作为完成凭证，没有发布后复测、重新构建、重复部署、读取 ZIP、计算哈希、反射 DLL 版本、重新下载、fetch 或远端页面复核。发布后仅归档文档，不修改被冻结的玩家说明、版本标签或安装包。既有实验失败、恶化个案及原始证据保留；本次未新增无头实例，未清理历史现场。
