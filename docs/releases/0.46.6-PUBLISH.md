# 0.46.6 发布与知识归档

[版本索引](README.md) · [完整中英说明](0.46.6-RELEASE_NOTES.md) · [准备记录](0.46.6-PREPARE.md) · [行为验证](../TEST_MATRIX.md#0466fork多人烘焙手套选牌计算2026-09-25)

2026-09-25，按用户“发布 release，然后用 neat-freak 归档”的请求，[GitHub Release 0.46.6](https://github.com/sgpsdd-zyx/CombatSolver/releases/tag/v0.46.6) 已创建为最新正式版，包含多人烘焙手套选牌计算修复。发布直接复用已完成的本地构建、部署和最小 ZIP。

## 发布凭证

| 阶段 | 状态与证据 |
| --- | --- |
| 来源 | `24954f785f21fb2e3be76372e36fdf00c07f539e`；官方基线仍是 `d231e9e5 / 0.46.4`。准备记录提交 `bb7b865e` 只改文档。 |
| 既有验证 | 烘焙手套原生选择后全队完整状态 0 差异；停止、队友原生出牌后重算、计算中异选与单人哨兵通过。Bash 结构门禁 `search_files=221` 通过，两平台规则同步。本次发布不重跑。 |
| 构建与部署 | 准备阶段正式构建 0 警告／0 错误、在线连接元数据通过；同次 DLL、manifest 与两份许可已部署到已确认的游戏 `mods/CombatSolver`。详见 `prepare-state.json`。 |
| 最小包 | `releases/CombatSolver-0.46.6.zip`，2,259,153 字节；四份确定输入，macOS 产物不含 Windows MemoryCleaner。直接上传，未重打包或复核包内容。 |
| 标签与分支 | 新建指向来源的 annotated tag `v0.46.6`；任务分支从 `1189a41c` 推至 `bb7b865e`，与标签一次原子推送成功；`push-release.log`。 |
| GitHub Release | 明确指定 `sgpsdd-zyx/CombatSolver`，使用 `--verify-tag`、`--latest`、现有 ZIP 及完整中英 `--notes-file`；`github-release-create.log` 返回本页 Release URL。非草稿、非预发布。 |
| 发布后归档 | 现役导航、规则、开发笔记和多人指南同步，新增本凭证；纯文档提交位于标签后，推送凭证为 `push-closeout.log`。 |

本地凭证位于 `.local/release-0.46.6/`，原始行为证据位于 `.local/multiplayer-toasty-20260925/`。各阶段采用成功命令作为直接证据，遵循仓库一次取证规则，没有再次构建、部署、测试、解包、读取 DLL 版本或发布后远端页面／下载复核。

## 六个事实面

| 事实面 | 终态 | 对齐与验证 |
| --- | --- | --- |
| 代码 | `verified-current` | 当前源码与准备产物的行为输入相同；版本 0.46.6，标签固定在 release source。当前提交只有文档归档。 |
| 运行态 | `changed-and-verified` | 本地部署沿用准备成功凭证；分支、标签和 GitHub Release 创建／资产上传成功。真实游戏可见界面和联机未验收，不能把渠道交付写成真人验收。 |
| 文档 | `changed-and-verified` | README 中英下载入口、文档／版本索引、开发笔记和多人指南统一为 0.46.6；准备阶段保留为明确历史。文档链接与状态检查见 `neat-doc-check.json`。 |
| 规则 | `changed-and-verified` | 项目唯一现役规则为根 `AGENTS.md`；用户级 Codex 规则为空，无项目 override 或下级规则。只更新版本入口并缩短手套摘要；技能和结构门禁沿用已验证实现。 |
| 记忆 | `out-of-scope`（`generated-read-only`） | 本机存在生成记忆目录，本轮没有获准的人工写入入口；不读取其他项目记忆或直接修改生成文件，现役结论保存在项目文档。 |
| 工作区 | `verified-current`；清场 `pending` | 盘点时 1 个 worktree、0 个未提交项；机械枚举 2,562 个非产物文件、444 份 Markdown，未发现一次性 PLAN／TODO 或旧副本命名候选。原始验证材料继续保留。 |

“已归档”表示现役知识与发布凭证已对齐，不表示原始材料已删除。规则文件体量仍约 29 KB，本机未配置 `project_doc_max_bytes`；达到 skill 所列默认 32 KiB 参考预算的约 89%，属于保留提醒。本次只缩短受影响摘要，未为压缩体量删除硬约束，也未改变全局配置。

## 上游策略影响说明

用户提出的单人／多人复用问题已补入[多人指南](../multiplayer-advisor.md#复用与单人隔离)：公共模拟、展开、去重、参数及部分估值会影响多人；贡献目标、前沿及明确隔离的单人策略分别说明。这是当前源码的文档归纳，不是本轮新增策略或性能结论。

## 复核现场与未执行项

只读清场预览记录于 `neat-residue-preview.json`。本轮可候选清理的是 `.local/multiplayer-toasty-20260925/controls-and-solo/`、`controls-and-solo-2/` 与 `fix-1/`：它们保留已在开发记录说明的启动／漏注册失败现场，最终替代证据已归档。尚未删除；基线、最终差分、最终控制与单人哨兵、原版参考和发布凭证继续用于复核。已清理的无头实例沿用测试启动器的成功记录，不重建或重复清理。

发布 ZIP、当前工作分支和唯一 worktree、受限私有配置、构建工具及其他批次历史不列入本轮删除范围。复核现场仍保留，等待用户确认后清场；本轮没有清场确认。

`out-of-scope`：真实网络联机、可见 Steam、后续回合／多名队友同时选牌、第三方组合、Windows/Linux 本机运行、完整回归和干净安装。多人 fork 的适用发布渠道为 GitHub，不使用官方创意工坊或夸克身份；不变更后台最新版提示。没有新的构建警告，规则体量提醒和待清场状态保留。
