# 0.43.5 发布与知识收尾

[返回版本索引](README.md) · [中英玩家更新日志](0.43.5-RELEASE_NOTES.md) · [合并与行为证据](../strategy/upstream-0432-merge-20260920.md)

2026-09-20（America/Los_Angeles）用户要求推送发布本次 release，并按 neat-freak 归档。[GitHub Release 0.43.5](https://github.com/sgpsdd-zyx/CombatSolver/releases/tag/v0.43.5) 已创建为正式最新版并上传最小 ZIP；命令成功时间为 2026-09-20 23:41:24 PDT（2026-09-21 06:41:24 UTC）。本记录说明发布交付，不代表真实联机或安装验收。

## 发布来源与阶段凭证

| 项目 | 已完成事实 |
|---|---|
| 发布仓库／分支 | `sgpsdd-zyx/CombatSolver`／`codex/multiplayer-advisor` |
| Release source | `d4371738faa21f79adc0a4e275ce807d5f1de089`，合入官方 `3f4002bd60ca9e232cd23191bc1c8eb1f60ec034 / 0.43.2` |
| 新标签 | annotated tag `v0.43.5` 固定在上述发布来源；不移动官方或既有 fork 标签，不补建中间准备版标签 |
| 已有构建／最小 ZIP | 复用 `.local/upstream-merge-20260920/package-receipt.json`；一次最终 Release 构建 0 警告／错误，`releases/CombatSolver-0.43.5.zip` 为 2,171,315 字节，包含 manifest、DLL、Windows 辅助程序及两份许可文件 |
| 玩家说明 | `691f0502` 汇总自已发布 fork 0.43.1 以来的十四周期、长线选路、遗物归属、佩尔之眼及上游更新，提供完整中英正文；没有改变构建输入 |
| 分支与标签 | 一次原子推送成功；`.local/release-0.43.5/git-push-release.log` |
| GitHub Release／附件 | 创建命令成功，使用已存在标签、完整中英说明和已有最小 ZIP；`.local/release-0.43.5/github-release-create.log` 与 `release-receipt.json` |
| 知识归档 | 发布后的文档、当前版本指针与规则作为独立提交同步任务分支，标签与二进制不变；最终推送凭证在现场 `git-push-closeout.log` |

正式多人 fork 沿用自己的 GitHub 渠道。上游 `tools/publish-release.ps1` 限定官方 `main` 和对应创意工坊／夸克身份，不用于本分支；已在根规则与 `release-gate` 中明确这个适用范围。官方创意工坊和夸克本次均未上传，状态为 `not-applicable`。

每阶段以成功命令为直接凭证，没有重复行为测试、构建、打包、读取 ZIP、部署、远端页面确认或下载复核。发布后只做知识文件的 L0 检查；不是完整发布门禁。

## 知识事实矩阵

本次完整盘点枚举 370 份 Markdown、一个工作树，发布前工作区没有未提交改动。盘点保存在 `.local/release-0.43.5/neat-inventory-before.txt`。根 `AGENTS.md` 为当前项目规则真身，Codex 全局同名文件为空，无项目 override；其他平台配置只列存在、不写入。

| 事实面 | 状态与依据 |
|---|---|
| 代码 | `verified-current`：定版源码、manifest、既有构建和包来源一致；本阶段没有行为改动，复用[测试矩阵](../TEST_MATRIX.md#0435fork官方-0432-合并2026-09-20)中的限定结论 |
| 运行态／发布 | GitHub 交付 `verified-current`；真实联机、可见 Steam、干净安装 `pending`，没有把发布成功当作实机通过 |
| 文档 | `changed-and-verified`：README、中英日志、版本索引、多人指南、研究索引、开发／测试记录统一到已发布 0.43.5；历史准备版和上游同号原文保持原语境 |
| 规则 | `changed-and-verified`：多人 fork 的 GitHub 发布渠道与官方 `main` 三渠道配置分开；必要检查、阶段复用、标签冻结与禁止可见 Steam 的约束保留 |
| 记忆 | `out-of-scope / generated-read-only`：不手改 Codex 生成记忆或全局配置，跨会话事实由项目现役文档承载 |
| 工作区 | `verified-current`：只提交本任务文档，构建、发布包、完整日志与官方源码导出继续由 Git 忽略；既有独立工具及原始材料不清理 |

发布前文档检查为 3 份 Markdown／15 个本地链接，`release-gate` 官方快速校验通过。发布后 10 份变更 Markdown／658 个本地链接／57 个锚点与版本对应通过；随后仅补记本段数字，不重复链接检查。工作区边界凭证见现场 `doc-validation-closeout.json`；未改职责或行为，不重跑结构门禁和游戏测试。根规则为 27,135 字节，约占默认 32 KiB 指令预算的 82.8%，未达到上限；本次缩减顶部历史叙事，不扩展为全仓规则重构。

## 保留的未验证项与现场

- 第三方 `ADAPTED-ONPLAY-INTEGRATION-CARD` 原生场景未返回协议结果；托管 40+2 项不能替代该项，原合并记录的 `pending` 保留。
- 真实联机、三／四人、全角色全遭遇、完整第三方 Mod 栈、可见性能与干净安装未验收。十四周期仍为规划上限，不保证长线安全或全局最优。
- 复核现场 `.local/upstream-merge-20260920/`、`.local/release-0.43.5/` 与历史研究／构建现场保留。发布包保留作交付，官方参考导出与中间构建只列为后续可评估的清理对象；本次不删除分支、工作树、原始问题包或临时资料，清场等待用户看完汇报后另行确认。
