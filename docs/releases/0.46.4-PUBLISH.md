# 0.46.4 发布记录

[版本索引](README.md) · [完整中英说明](0.46.4-RELEASE_NOTES.md) · [合并与知识归档](../strategy/upstream-0463-merge-20260924.md)

2026-09-24 用户要求合并官方新更新、neat-freak 归档并发布 fork Release。[GitHub Release 0.46.4](https://github.com/sgpsdd-zyx/CombatSolver/releases/tag/v0.46.4) 已创建为正式最新版，上传最小 ZIP；任务分支和 annotated tag 已原子推送，正式构建已部署到本机游戏 Mod 目录。

## 发布来源与直接凭证

| 阶段 | 完成证据 |
| --- | --- |
| 来源 | `441fad36d396901467b1a8a10b2ec89b08e73b93`，合并官方 `72295691457ec71eef9bf2d53c27f6b25d2c9f90 / 0.46.3`；前一 fork 公开版本为 0.46.3。 |
| 版本与标签 | `v0.46.4` 为指向来源提交的新 annotated tag；现有 fork 0.46.3 标签与官方同号记录保持。官方 0.46.3 玩家日志另存 `upstream/`。 |
| 行为验证 | 复用[本轮结构化证据](../strategy/upstream-0463-merge-20260924-evidence.json)：60 项运行库合同、七个 macOS 原生请求、两根 231 字段官方对照、1000 节点增量回放及 Bash 职责门禁。发布阶段没有重跑场景。 |
| 私有配置 | 复用前次发布已确认的受限客户端连接 props；本轮不重新提取、不输出值、不访问在线端点。配置目录继续被 Git 忽略。 |
| 正式构建 | 来源提交一次 Release 构建，启用 `PublicationBuild=true` 并提供私有配置目录，0 警告／0 错误，8.46 秒；`build.log`、`build.json`。 |
| 新 DLL 配置 | 五项客户端连接元数据存在，HTTPS 和证书指纹格式通过；只读取指定元数据，不加载程序集、不读取 DLL 版本、不请求服务；`connection-final.json`。 |
| 本地部署 | 同次新 DLL、manifest、LICENSE、THIRD_PARTY_NOTICES.md 精确复制到已确认的 `SlayTheSpire2.app/Contents/MacOS/mods/CombatSolver`，不清空目录或修改其他 Mod；`deployment.json`。 |
| 最小 ZIP | `releases/CombatSolver-0.46.4.zip`，2,249,615 字节，由上述四份确定输入一次写入；`preparation.json`。macOS 不构建 Windows MemoryCleaner。 |
| 分支与标签 | `codex/multiplayer-advisor` 从 `1532fd50` 推送至 `441fad36`，同时发布 `v0.46.4`；`git-push-release.log`、`tag.json`。 |
| GitHub Release | 显式指定 `sgpsdd-zyx/CombatSolver`，验证现有标签，用完整中英玩家说明和最小 ZIP 创建正式最新版；`github-release-create.log` 返回 Release URL。 |
| 发布后知识归档 | 当前入口与合并／测试记录同步发布状态，纯文档提交位于版本标签后；推送凭证为 `git-push-closeout.log`。 |

本地凭证目录为 `.local/release-0.46.4/`，行为证据目录为 `.local/upstream-update-20260924/`。正式构建只因加入发布连接元数据而与开发验证构建不同，未改变行为源码、测试输入或依赖。

发布前 L0 检查覆盖当时 45 个任务路径、927 个文档文件链接及 5 个 JSON，无失效链接、重复键或未闭合代码围栏；其后新增 coverage 索引和更新证据 JSON 另通过唯一键解析。双语玩家说明各三项，贡献者与 PR 链接均保留。此前 fork 0.46.3 与官方同号更新日志逐份保持其来源原文。

## 未验证与保留范围

真实联机、可见 Steam／动画／性能、Windows 与 Linux 原生测试、DOP>1 搜索质量、完整回归／干净安装和在线服务可用性未验收。官方历史中的一根战损恶化、StandPat DOP 计数合同失败及 BeamRankSortChecks 既有问题保留；两根本机对照不表示普遍质量保证。

本分支不发布官方创意工坊或夸克渠道，不修改后台最新版提示。每阶段成功命令即完成证据；没有发布后复测、重复构建／部署／打包、ZIP 解包／枚举、哈希／DLL 版本反射、远端页面或下载复核。

neat-freak 的文档与规则同步已完成，生成记忆保持只读。原生测试实例已按仓库规则清理，复核源码与原始证据仍保留；证据清场等待用户看完汇报后明确确认，范围见[归档清单](../strategy/upstream-0463-merge-20260924.md#保留项与限制)。
