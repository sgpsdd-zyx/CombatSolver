# 官方 0.44.0 合并与第一轮知识归档

[返回策略索引](README.md)

本批按用户要求先合并官方更新、归档，再实施独立多人贡献目标并统一发布。起点为 fork `bdbf2143`，合入官方 `42e0902874f5f3c329ef13dccf30eb694375ca17 / 0.44.0`；官方标签只读，保存在独立上游引用。此阶段版本仍为已发布 fork 0.43.6，未创建新的标签、ZIP 或 Release。

## 合并取舍

- 官方单人更新保留：余像前移、循环搜索与请求级重放额度、历史依赖掩码、搜索组合预算调整、回合开始 Power 通知、无效插入药水后缀拒绝、GC 完成链修复与循环 UI。
- 多人显式绕过余像审计、单人循环重放、能力承诺和单人成长目标；合并阶段保留原格挡探索值。新的单人长期格挡计价不直接成为多人政策。
- 历史依赖掩码复用官方根扫描；获取历史值时，单人消费原累计值，多人继续按各效果原持有者扫描。完整全队根、死亡/复活 Hook 资格和多人遗物持有者展示保留。
- 生成怪物的冻结站位编号与循环显示身份合入，同时保留遗物的玩家编号。后台不读取实机节点。
- 官方同号日志原文归入 [0.43.3](../releases/upstream/0.43.3-RELEASE_NOTES.md) 与 [0.44.0](../releases/upstream/0.44.0-RELEASE_NOTES.md)，不覆盖 fork 0.43.3；导入的开发/测试章节明确标为上游历史。

## 本轮直接证据

证据目录 `.local/upstream-0440-merge-20260922/` 被 Git 忽略并保留。下表为当前合并源码的结果，不借用上游历史通过记录。

| 检查 | 结果 |
|---|---|
| 合并版 Release、离线宿主 | 两项构建均 0 警告、0 错误 |
| 独立官方 0.44.0 Release 与宿主 | 从官方提交导出构建，两项均 0 警告、0 错误 |
| 单人同根 Coordinator 对照 | 92 项非时序字段及完整路线一致；双方 1750 展开、4363 转移、2 HP 战损；多人四入口均为零 |
| 多人 `upstream-compatibility` | 13 项通过；原持有者历史、金斧、能力归属、Fork/生产键、根不变 |
| 多人 `dead-teammate` | 死亡/复活、主机与非零本机索引、手动重算搜索、后续周期通过 |
| 循环展示纯合同 | 125473 项断言通过；不代替真实可见 UI |
| 原生 `UI-LOCALIZATION` | eng/zhs/zht、452 项目录、四只扭动虫左起编号、Fork/死亡稳定、循环宽窄布局和遗物持有者通过 |
| 原生 `LOOP-HISTORY-DEPENDENCIES` | 33 项通过；手/抽/消耗牌堆、未来生成来源、Fork、历史差异及并发请求额度 |
| Bash 职责门禁 | `REFACTOR_BOUNDARIES_OK search_files=215`；Windows 等价规则同步，未在本机执行 PowerShell |

原生请求启用 `COMBATSOLVER_VERIFY_FAST_LANES=1`，同一隔离进程复用两项通过测试。启动器成功删除实例目录。

## 失败与未验证

- `ARSENAL-HAND-DRAW-SHUFFLE-CHOICE-REPLAY` 首次默认 1 HP 敌人过早结束，未覆盖回合前缀复用；调整为 200 HP 和九张牌后在 120 秒内未返回。未扩大超时，第二实例也已清理。此夹具本轮未通过，不将它写成回合开始 Power 修复的本机语义验收。
- `CombatSolver.GcPolicyChecks diagnostic-failure` 在进入实际 No-GC 区域的前置断言失败；后面的异常诊断完成链未获得本机执行证据，不按上游八项通过计数。
- 无可见 Steam、真人主机/客户端联机、全角色回归或通用性能结论。上述单人一致性只覆盖所列同根哨兵。

## neat-freak 真相矩阵

| 事实面 | 状态、来源与动作 |
|---|---|
| 代码 | `verified-current`：官方合并与上述直接验证；多人新策略尚未实施 |
| 文档 | `changed-and-verified`：指南、架构、总/策略/发布索引同步新官方基线；旧证据与已发布 0.43.6 保留历史身份 |
| 规则 | `changed-and-verified`：根 AGENTS 为唯一仓库入口，无子目录 override；Search skill 与双平台门禁同步显式多人隔离 |
| 运行态/发布 | `pending`：尚未安装或发布此合并；用户要求新策略完成后统一定版 |
| 记忆 | `generated-read-only`：未写入 Codex 自动记忆；其他平台规则不在修改范围 |
| 工作区 | `verified-current`：初始单一工作树无用户脏改动；全部文件/规则已机械盘点，构建产物、官方导出与日志保留在忽略目录 |

根 AGENTS 超过默认 32 KiB 预算的 70%，本轮只替换现役摘要，不追加历史长段；准确读数在本地归档清单。独立尖塔军师工具、旧研究原文及其他平台规则为 `out-of-scope`。不清场、不删除历史复核现场；无需要阻塞开发的用户决定。

下一阶段按[本机贡献目标研究](multiplayer-cooperative-planning-20260922/README.md)替换 3 HP 优先，不预测队友主动行动，保持官方单人隔离。发布后的第二轮归档另记最终实现与交付。
