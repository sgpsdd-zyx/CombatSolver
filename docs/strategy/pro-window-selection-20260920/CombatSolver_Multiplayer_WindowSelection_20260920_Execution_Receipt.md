# 实际运行次序

Python环境：`3.13.5`（CPython）。

1. 创建只在源码目录外运行的标准库实验脚本。
2. 初次执行成功写出结果后，内容检查发现缩减模型的D预算与显式兑现等问题；初版记录移入development，未计为最终有效对照。见corrections.txt。
3. 修正后执行核心脚本一次：132核心、96不同预算、3权重、18微型检查，真实stdout核心段记录本次墙钟。
4. 新增而非重跑：W39目标机会成本、W40便宜/错误尾部、W38的378/382/386/394断点共38次。通过run_new_supplement.py调用最终脚本函数一次，将新结果合入JSON、stdout追加。最终公开脚本的默认main会一次运行全部最终测试。
5. 输出中文文档、读取清单、资料记录；一次静态交付核查，不为安心重复通过的实验或重新全量校验输入源码。

本轮没有dotnet、游戏DLL、生产C#、原生实际/模拟差分或联机执行。没有仓库实现、推送、发布。269是最终搜索运行数，不含18项微型检查内部的720排列和216三元枚举，也不把修正前执行累计成有效样本数。

交付前补充限制说明：W27仅验证计数分离，不验证真实额外回合签名。只增加解释性元数据与文档，未修改数值结果、未重复运行已经通过的实验。
