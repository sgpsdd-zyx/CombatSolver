# 多人未击杀路线与长线选路专项研究请求

日期：2026-09-20。交给用户此前指定的 ChatGPT 6 Pro 会话。本文是研究输入，不是生产策略变更或发布指令。

## 用户目标与固定基线

用户原始描述：

> 这个是值得探究的问题，虽然队友的行为会对后续形态造成影响，但我们也得尽量调整牌序，保战损同时多打出伤害，只比较第一周期我认为不太合理，你可以继续和之前一样，借助6 pro，探究一下这个问题，看看有没有什么更好的策略

唯一当前源码为本次附件 `CombatSolver-851c1521-source.zip`，对应提交 `851c1521f5258ec43dc71c24eeb8d5200fdf612d`、本地准备版 `0.43.3`。请独立解压，不能把会话中旧附件的 `0.41.2`、`3ccac17` 或七周期当作当前实现。本次提交尚未推送；GitHub 的旧源码不能替代附件。

近期已完成：官方 `cccc270 / 0.43.0` 合并；多人默认上限从 7 增至 14 个敌方周期；普通多人请求时间、节点额度各乘二，按剩余敌方周期分摊；佩尔之眼多人额外回合状态和遗物显示归属修复。终局事实、多人早停隔离、根前本机用药与最终资格/固定批次已修。当前正式同分项仍使用完整动作数，共同周期动作成本只保留离线实验。

## 要解决的问题

多人高血量战斗常在本次搜索内没有完整击杀解。用户愿意多花计算时间，希望当前的牌序、目标、选牌、用药和铺垫能在防守约束下带来更多后续实际伤害。真人队友未来动作未知不意味着只能优化第一周期，也不意味着可以假定其会配合。

需要研究的是未结束路线的比较目标、证据深度和计算分配，不是再次争论固定 7 或 14 谁更优。14 是本轮上限，允许研究在此范围内改变选路/分配；维持本机手动请求、手动操作、每敌方周期 3 HP 目标、单人完全隔离。禁止把未知后续当作安全证明。更高伤害、少战损与救援之间的政策变化必须单列，不能隐藏在启发式总分里。

## 本地已确认的源码事实，仍请独立核对

- `src/Search/CombatBeamSolver.Multiplayer.cs` 的 `PrepareMultiplayerFinalCandidates` 先检查用药资格，固定整批共同比较周期，排序后保留 `4 * BeamWidth`；预览/最终选路共享同一上下文。
- `src/Search/CombatBeamSolver.MultiplayerEvaluation.cs` 的 `CreateMultiplayerOrdering` 从未胜利/死亡、未被外部选择阻断且有周期检查点的候选中取最浅完成周期。若该类候选不存在才用阻断候选的检查点回退；全终局池的周期值可以只是上限哨兵。零检查点候选不总是把共同深度变为零。
- 未结束路线的收益在共同周期读取：敌方有效剩余 HP、本人扣血/生命、队伍存活人数、用药。真实胜利/死亡读取完整终局。后续已知死亡、保命资源消耗、超额扣血与队友死亡不能被旧检查点隐藏。
- 比较顺序依次是用药要求、本机死亡、完整可比事实、保命资源消耗、超出每周期 3 HP 的累计部分、胜利、队伍存活人数、敌方有效剩余 HP、本人累计扣血、本人生命、用药数量、双方胜利时结束回合、完整路线动作数。无完整可比事实时用评分与动作数回退。
- 因而，同一首周期结果下，较深路线后续伤害不加分，后续动作数却可能使其落后；深层风险仍可使它落后。当前这是有限证据政策，不能直接称为全局最优或安全证明。
- `src/Search/CombatBeamSolver.Phases.cs` 在预算停止时把 `advisoryLastCohort` 加回最终池；用药/无药兜底也可能引入更浅路线。请沿实际调用链确认其发生条件、压缩时点、混深来源，不把人工池反例冒充生产可达。
- `SearchedTurns` 来自最后选中动作序列；它不等于实际搜索最深层。UI 分别展示选中路线周期、窗口上限和共同比较周期。

这些事实支持“可能偏向短路线”，尚未复现用户具体实战局。请主动纠正不准确的推断，不以迎合本地建议为目标。

## 必须比较且尝试推翻的候选

1. A：现役共同最浅周期与完整动作数，作为对照。
2. B：只把末级动作数改为共同周期内成本。预期只能消除同分偏差，请验证是否完全不改善当前首动作。
3. C：按完整敌方周期分组、逐层维护可发布结果；预算停止时选最后有足够候选覆盖的完成层。明确“足够”如何定义、费用如何计入、是否偏爱分支少的路线，以及尚未伸展的强首动作如何处理。不能只是最长优先。
4. D：对有代表性的首回合计划/动作族，用同一个后续策略与剩余额度补齐可比边界。所有模拟、选择、回放和重算计费；覆盖末节点、强制药水、额外回合、兄弟分支、外部选择和预算耗尽。主动构造它抢走主搜额度或错误续行导致退化的反例。
5. E：多个后续时点的实证收益曲线，或可解释的风险约束/稳健性评价。区分敌方 HP、实际累计有效伤害、击杀、控制、资源和本人/全队贡献。不得把名义伤害、过杀、无穷治疗刷伤害、深度、未兑现铺垫直接加成。时间折扣/权重须有明确含义，并给敏感性与反例。

也可以提出更简单的新方案，但要与上述最小方案比较成本。MCTS、PUCT、学习价值函数、队友意图推断不是默认答案。

## 队友未知与目标定义

- 当前生产模型只主动规划本机；队友已有被动效果与敌方结算照常，不虚构其未来主动出牌。
- 请辨别“队友不主动行动”是不是下界；目标切换、怪物阶段、复活、击杀触发、治疗和 RNG 都可能使该模型并非严格保守。
- 先评估不依赖队友配合的本机收益：费用/抽牌顺序、独立能力成长、延迟攻击和资源兑现；再评估队友提前击杀目标或提前结束战斗、提供防守、改变怪物状态等情景。
- 队友情景只能作为敏感性测试或明确假设，不能伪造概率分布，不将平均分写成真实联机胜率。默认政策是否需要情景模拟必须有付费后的收益证据。
- 当前“每周期 3 HP”是排序目标，不是过滤掉所有超额候选的绝对可行域。若建议变为硬门槛、预算化约束、Pareto 或软约束，请说明超额无解、保命/救援和已有根扣血如何处理，作为独立可选政策，不顺带实现。

## 实验与证据要求

请真正运行可证伪的实验，不只写方案。可以用标准库 Python 缩减模型，但明确与 C# 的差异。至少覆盖：

- 深层有利路线被浅兜底拉回周期 1，以及首周期相同但后续动作变长的同分偏差。
- 首周期少伤害换第 3/7/14 周期收益、顺序相反造成亏损、多步组合第三步才兑现。
- 深路线已暴露风险、浅路线未知；同首动作有好坏两个续行，不能因一条坏续行封禁整个首动作。
- 某候选低分支快速变深，强候选高分支未完成；只取最深层的选择偏差。
- 额外玩家回合不等于敌方周期；队友选择阻断；强制用药尚未完成；提前终局事实优先。
- 相同预算下补齐续行或多时点计算反而挤占更有价值搜索的负例；峰值保留量和取消/排空成本。
- 队友提前击杀/防守/目标变化，不能只做有利扰动。多敌、恢复/阶段、反复伤害计分的反例。

对每个策略固定相同根、总逻辑工作预算和随机种子。独立记录上限、实际展开分布、已生成最深周期、选中路线周期、共同比较周期、当前完整首回合动作、模拟/回放/候选扫描成本。质量按统一外部评价时点（建议 3/7/14 周期）与相同后续协议评估，不能把各自不同终点的累计伤害直接比较。首动作不变但后缀更长只记证据覆盖，不能记为当前决策改善。外部评估工作不得伪装成搜索内免费信息。

复用源码里的 `tools/OfflineSearchHarness/MultiplayerHorizonContracts.cs`、`MultiplayerFinalSelectionContracts.cs`、`MultiplayerReviewContracts.cs` 和旧研究反例。先读实现再决定是否复跑旧模型；没有游戏 DLL 或 .NET 时明确未运行生产 C#。请给可供本地接入的最小探针设计，不输出声称已运行的伪 C# 成功结果。

请查阅相关原始论文/官方资料（例如 anytime/iterative deepening、有限视界规划、rollout、部分可观察多智能体决策），引用支持具体取舍的原文来源；理论前提与本问题不一致时明确指出。避免用通用算法名单替代具体设计。

## 交付

请持续完成研究，不只回复计划。最终至少交付：

- `CombatSolver_Multiplayer_WindowSelection_20260920_Review.md`：当前代码事实、可达性/假设、方法与物理行号、主结论、反对证据、原始资料和未验证范围。
- `CombatSolver_Multiplayer_WindowSelection_20260920_Design.md`：一个明确主方案、一个低成本回退、完整伪代码、目标/风险次序、预算与所有权、最多两批最小落地计划和撤回条件。
- `CombatSolver_Multiplayer_WindowSelection_20260920_Experiments.py`、`Results.json` 和真实 stdout。
- 源码实际读取范围与完整证据 ZIP。只需对本次输入/交付各保留一次直接凭证，不为安心重复运行已通过的同一实验。

不要修改、推送或发布仓库。不能保证方案全面正向时，给出明确代价和适用边界。若无法支持替换当前默认策略，也须指出下一项最有区分力、且本地可执行的实验，而不是笼统要求更多数据。
