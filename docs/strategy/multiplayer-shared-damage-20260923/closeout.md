# 0.44.2 知识归档（2026-09-23）

[策略索引](../README.md) · [实施与实验](implementation.md) · [结构化证据](implementation-evidence.json) · [本地定版](../../releases/0.44.2-PREPARE.md)

本次按 `neat-freak` 完整路径做当前任务的知识收尾。行为来源为 `fd976376c79cf87561d13397f2c5ddc330cc80ae`，本地包为 `0.44.2`；最近对外发布仍为 `0.44.1`。仅同步文档、规则与证据解释，没有新增行为、发布或清场。

## 已对齐的事实

| 主题 | 权威来源 → 原有缺口 → 本次处理 |
| --- | --- |
| 交付阶段 | 已有构建/打包成功凭证 → 正式文档只写“本地准备” → 增加本地定版记录与结构化 `localPackage`，保留来源提交，不重复构建或开包。 |
| 实施范围 | 当前多人计数与评分代码、原生/政策合同 → 容易把选路折算理解成个人伤害归属 → 指南、版本说明与规则统一为“独立选路信用，个人贡献保持真实归属”。 |
| 实验可信范围 | `MultiplayerRaceExperiments` 的建局、回放及停止条件 → “实际外评/胜负”容易被理解为原生整场 → 明写合成小牌组、共用模拟器、首轮后固定续行、没有逐牌交错/重算闭环、截断不等于团队失败。 |
| 否定性结论 | 两个原型在指定夹具中没有改变结果 → 容易推广成策略无用 → 统一为“当前实验未检出收益”，保留原型供独立后续研究。 |
| 规则入口 | 根 `AGENTS.md` 与现役指南 → 顶部积累多次发布和测试叙述 → 压缩历史，保留行为/授权硬边界，版本事实指向版本索引。 |
| 证据数目 | 71次变体搜索和14次窗口搜索 → 容易叠加成独立战斗样本 → 索引、宿主说明、开发笔记、测试矩阵与结构化证据统一口径。 |

历史静态审视原文、原型快照及实验数值没有改写。原生20项差分和政策合同的 Passed 保持其原有覆盖范围；不据此保证真人联机胜率、可见性能、所有角色或完整战斗质量。

## 六个事实面的状态

| 事实面 | 状态 | 证据与未闭合范围 |
| --- | --- | --- |
| 代码 | `verified-current` | 当前实现与既有直接证据一致；本次没有源码变更或新行为测试。 |
| 运行态 | 本地交付 `verified-current`；实机 `pending` | Release与ZIP创建成功已归档；安装、真人联机、可见性能未验收。外部发布不在本次范围。 |
| 文档 | `changed-and-verified` | 指南、实验、版本索引、开发记录、测试矩阵、结构化证据与本记录同步；L0路径/链接与JSON核对。 |
| 规则 | `changed-and-verified` | 当前项目只有根 `AGENTS.md`，无子目录覆盖或项目 `CLAUDE.md`；本机 Codex 全局规则文件为空。未创建额外规则副本。 |
| 记忆 | `out-of-scope`，`generated-read-only` | Codex生成记忆目录存在；短索引定向检索没有本项目命中，不等于整个记忆库已审计。没有改写生成记忆或其他平台记忆。 |
| 工作区 | 当前任务盘点 `verified-current`；清场 `out-of-scope` | 盘点时一个工作树、任务分支无未提交改动。原始实验、旧DLL和本地包全部保留；未逐项审计其他历史 `.local` 材料。 |

只读盘点在编辑前枚举2329个项目文件、410篇Markdown；没有加载反编译目录或构建目录内容。日志位于 `.local/neat-multiplayer-20260923/`。规则文件从28,743字节压缩至27,850字节；按skill引用的默认32KiB项目指令预算参考，仍约85%，超过其70%提示线。本次只压缩重复历史，未修改平台预算或删除仓库硬约束。

## 现场保留与清场预览

| 本地材料 | 盘点结果与处理 |
| --- | --- |
| `.local/multiplayer-race-20260923/baseline/` | 约6.48MB、2文件；冻结旧DLL与来源记录，后续A/B仍需要，保留。 |
| `native-shared/`、`native-ui/` 及 `final-*` | 原生/最终默认直接证据，保留。两次无头实例已由当时启动器删除，此次未重新启动或核验实例。 |
| `prototype/` 与开发/留出实验目录 | 保留具体复核现场；正式文档有结果摘要与原型快照，不能据此假定所有原始文件可删。 |
| `coverage-check/` | 约14.92MB、1495文件；隔离生成副本，是以后可复核的清理候选，本次未删除。 |
| `releases/CombatSolver-0.44.2.zip` | 已完成的本地交付物，保留；不重新解包或创建。 |

没有额外工作树需要移除。发布包、完整日志与构建产物没有进入源码提交。复核现场仍保留，等待用户确认后清场；这不阻碍本次知识归档和下一步实验设计。

## 本次验证口径

只执行与文档相称的 L0：15个改动文件涉及的771个本地Markdown目标、59个锚点及JSON解析通过，历史实验/合同结果字段保持原值，Git差异检查通过。凭证 `.local/neat-multiplayer-20260923/l0-validation.json`。行为测试、Release构建与ZIP创建复用原有成功记录；没有把静态检查记成新增原生通过。下一轮实验需先明确队友策略、交错操作、重算节奏、团队终局和独立样本，再决定实施范围。
