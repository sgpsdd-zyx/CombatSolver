# 被拒绝原型快照

[返回实施记录](../implementation.md)

这是 2026-09-23 实验中间版本的源码材料，未进入生产，不作为现役规则或新的实施授权。

- `evaluation.patch`：相对 `cad3ba35` 的跟踪文件差异，保留共享计数、原型评分、前沿保护与宿主路由；移除了仅供本轮原生 fixture 使用的 Testing 分派差异。
- `MultiplayerEvaluationPolicy.cs`：原型枚举、敌方生命向量、威胁权重实现；当时位于 `src/Search/`。
- `MultiplayerRaceExperiments.cs`：五变体固定预算和外部队友脚本；当时位于 `tools/OfflineSearchHarness/`。

在独立的 `cad3ba35` checkout 应用 patch，并将两份新文件复制回上述位置，可重建快照设计。重建步骤本轮没有再次执行，不声称归档包已单独验收。`docs/**/*.cs` 已由项目排除，不能将研究文件直接纳入生产。

特征 0 为原策略，1 为共享伤害，2 为队友死亡代价按死亡周期除权且移除尾部队友死亡代价，4 为按根怪物未来意图伤害给各敌人剩余生命加权，7 为三者合并。队友首周期死亡仍保留完整代价；威胁权重使用冻结根，未知新敌人使用原型显式的总体权重。2/4 没有观察到选路收益，最终仅保留1。

最早探索版本还没有前沿目标代价保护；本快照是其后补齐保护和 AnyPlayer 辅助目标的版本。早期数据与本快照不逐字对应，完整数据按运行目录标明，最终验收另列。不要用原型快照的结果替代当前默认 DLL。
